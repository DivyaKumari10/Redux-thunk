export const mid1=store=>next=>action=>{
    // console.log('mid1 running.....');
    return next(action);
}
export const mid2=store=>next=>action=>{
    // console.log('mid2 running.....');
    return next(action);
}